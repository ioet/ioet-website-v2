import T from "../../theme"

const defaultStyles = {
  root: {
    background: T.palette.gradient.ioetOrange,
    height: "20%",
    width: "100%",
    position: "relative"
  },
}

export default defaultStyles
