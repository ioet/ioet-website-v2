import { defaultText, defaultGrid } from "../../../maps/defaultStyles"

const defaultStyles = {
  root: {
    marginBottom: 0.5,
    padding: 2,
    margin: "auto",
  },
  img: {
    margin: "auto",
    display: "block",
    maxWidth: "100%",
    maxHeight: "376px",
  },
  text: defaultText,
  grid: defaultGrid,
}

export default defaultStyles
